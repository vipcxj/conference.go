#include "impl/subscribation.hpp"

namespace cfgo
{
    namespace impl
    {
        Subscribation::Subscribation(const std::string& sub_id, const std::string& pub_id): subId(sub_id), pubId(pub_id) {}
    } // namespace impl
    
} // namespace cfgo
